..
    extra content for lapyx.components

The ``lapyx.components`` submodule provides a number of helper classes to make handling various LaTeX features easier and more programmatic. These are all imported by default, so you don't need to import them explicitly. Below are some simple examples of each.

.. include:: helper_classes.rst